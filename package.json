{
  "name": "milestonepay",
  "private": true,
  "packageManager": "pnpm@12.4.1",
  "engines": { "node": ">=22.18.0" },
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test"
  }
}
